# Dependencies needed to run the test suite in CI.
#
# Derived empirically: installed into a clean venv until `pytest -q` matched the
# local result exactly (1334 passed, 15 skipped). Unpinned on purpose -- CI should
# surface upstream breakage rather than hide it behind a lockfile. Pin here if a
# release ever breaks the build.
#
# NOT listed, deliberately:
#   structlog             -- impact_slides/logging_setup.py soft-imports it and falls
#                            back to a stdlib adapter. tests/test_logging.py asserts
#                            via caplog, which only sees the stdlib path, so INSTALLING
#                            structlog fails 7 tests. Pre-existing test gap, not a CI
#                            concern; see the note in the CI PR before "fixing" it by
#                            adding the dep.
#   sentence-transformers -- only reached by optional semantic-dedup paths, which the
#                            suite skips. Multi-hundred-MB torch install for no coverage.
#   playwright            -- screenshot tooling in scripts/, not exercised by tests.

pytest
pydantic
pandas
numpy
PyYAML
python-pptx
python-docx
beautifulsoup4
lxml
rapidfuzz
json-repair
pdfplumber
pymupdf
pillow
pytesseract
openpyxl
xlrd
