# Dependencies needed to run the test suite in CI.
#
# Derived empirically: installed into a clean venv until `pytest -q` matched the
# local result exactly (1334 passed, 15 skipped). Unpinned on purpose -- CI should
# surface upstream breakage rather than hide it behind a lockfile. Pin here if a
# release ever breaks the build.
#
# structlog is included as of #129: logging_setup.py soft-imports it and prefers it
# when present, so CI must exercise that path. It previously failed 7 tests because
# they asserted through caplog, which cannot see structlog's stderr sink -- those
# tests now assert through run.log, the sink both backends share.
#
# NOT listed, deliberately:
#   sentence-transformers -- only reached by optional semantic-dedup paths, which the
#                            suite skips. Multi-hundred-MB torch install for no coverage.
# Playwright is required for renderer audits.

pytest
pydantic
pandas
numpy
PyYAML
python-pptx
python-docx
beautifulsoup4
lxml
rapidfuzz
json-repair
pdfplumber
pymupdf
pillow
pytesseract
playwright
openpyxl
xlrd
structlog
