# renderer_v3_file_architecture

## Purpose

Archify per-file architecture diagrams for `impact_slides/renderer_v3/*.py` (HTML + JSON + visual-check PNGs).

## Ownership

- One stem per source file (`charts`, `plan`, `publish`, …) plus `index.html`.
- Inputs are the tracked Python modules; these files are generated evidence, not runtime.

## Local Contracts

- Do not treat these diagrams as the live engineering contract; code + `impact_slides/renderer_v3/AGENTS.md` win.
- Regenerating a file overwrites that stem; keep `index.html` in sync with the set.

## Work Guidance


## Verification


## Child DOX Index

- No child AGENTS.md.
